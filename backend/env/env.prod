# Environment configuration for Web Notes backend

# JWT Configuration
ACCESS_TOKEN_EXPIRE_MINUTES=1440
HOST=0.0.0.0
DEBUG=True

# CORS Configuration
# Use regex to allow chrome extensions and all Cloud Run domains
ALLOWED_ORIGINS_REGEX="(chrome-extension://.+|https://([a-z0-9-]+\.)+run\.app)"
